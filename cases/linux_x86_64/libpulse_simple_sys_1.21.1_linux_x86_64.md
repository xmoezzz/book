# `libpulse-simple-sys` `1.21.1`

Platform: Linux x86_64

## Build-level coding evidence

### pkg-config / pkgconf

Working directory: `/work`

```text
pkg-config --libs --cflags libpulse-simple
```

Working directory: `/work`

```text
pkg-config --modversion libpulse-simple
```

Working directory: `/work`

```text
pkg-config --libs --cflags libpulse-simple "libpulse-simple >= 5.0"
```

Working directory: `/work`

```text
pkg-config --modversion libpulse-simple "libpulse-simple >= 5.0"
```
