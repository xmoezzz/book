# `soup3-sys` `0.5.0`

Platform: Linux x86_64

## Build-level coding evidence

### pkg-config / pkgconf

Working directory: `/work`

```text
pkg-config --libs --cflags libsoup-3.0 "libsoup-3.0 >= 3.0"
```

Working directory: `/work`

```text
pkg-config --modversion libsoup-3.0 "libsoup-3.0 >= 3.0"
```
