# `pcap` `2.3.0`

Platform: Linux x86_64

## Build-level coding evidence

### pkg-config / pkgconf

Working directory: `/work`

```text
pkg-config --libs --cflags libpcap "libpcap >= 1.0.0"
```

Working directory: `/work`

```text
pkg-config --modversion libpcap "libpcap >= 1.0.0"
```
