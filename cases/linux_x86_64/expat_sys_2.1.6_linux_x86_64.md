# `expat-sys` `2.1.6`

Platform: Linux x86_64

## Build-level coding evidence

### pkg-config / pkgconf

Working directory: `/work`

```text
pkg-config --libs --cflags expat "expat >= 2.1.0"
```

Working directory: `/work`

```text
pkg-config --modversion expat "expat >= 2.1.0"
```
