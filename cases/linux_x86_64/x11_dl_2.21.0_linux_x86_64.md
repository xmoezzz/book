# `x11-dl` `2.21.0`

Platform: Linux x86_64

## Build-level coding evidence

### pkg-config / pkgconf

Working directory: `/work`

```text
pkg-config --variable=libdir xext
```

Working directory: `/work`

```text
pkg-config --variable=libdir gl
```

Working directory: `/work`

```text
pkg-config --variable=libdir xcursor
```

Working directory: `/work`

```text
pkg-config --variable=libdir xxf86vm
```

Working directory: `/work`

```text
pkg-config --variable=libdir xft
```

Working directory: `/work`

```text
pkg-config --variable=libdir xinerama
```

Working directory: `/work`

```text
pkg-config --variable=libdir xi
```

Working directory: `/work`

```text
pkg-config --variable=libdir x11
```

Working directory: `/work`

```text
pkg-config --variable=libdir x11-xcb
```

Working directory: `/work`

```text
pkg-config --variable=libdir xrandr
```

Working directory: `/work`

```text
pkg-config --variable=libdir xtst
```

Working directory: `/work`

```text
pkg-config --variable=libdir xrender
```

Working directory: `/work`

```text
pkg-config --variable=libdir xscrnsaver
```

Working directory: `/work`

```text
pkg-config --variable=libdir xt
```
