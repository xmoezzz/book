# `lzma-sys` `0.1.20`

Platform: Linux x86_64

## Build-level coding evidence

### pkg-config / pkgconf

Working directory: `/work`

```text
pkg-config --libs --cflags liblzma
```

Working directory: `/work`

```text
pkg-config --modversion liblzma
```
