# `libpulse-sys` `1.21.0`

Platform: Linux x86_64

## Build-level coding evidence

### pkg-config / pkgconf

Working directory: `/work`

```text
pkg-config --libs --cflags libpulse
```

Working directory: `/work`

```text
pkg-config --modversion libpulse
```

Working directory: `/work`

```text
pkg-config --libs --cflags libpulse "libpulse >= 8.0"
```

Working directory: `/work`

```text
pkg-config --modversion libpulse "libpulse >= 8.0"
```
